/**Escreva uma arrow function que receba um objeto com informações de uma pessoa 
(nome, idade, sexo) e retorne uma string no formato "Nome: [nome], Idade: [idade], Sexo: [sexo]".*/

const pessoa = { nome: 'Lorax', idade: 24, sexo: 'Feminino' }
const info = cliente => string = JSON.stringify(cliente)
info(pessoa)
console.log(string)